-- JSYS was here! :P

Config = {
    KeyBinding = 0x00000000, -- key hash
    Range = 5, -- meters
    Cooldown = 10, -- seconds
    TickerCheck = 100, -- milliseconds
    VisibleDuration = 10, -- seconds

    CoordinatesAll = {
        vector3(10, 10, 1), -- x, y, z
        vector3(10, 20, 3), -- x, y, z
        vector3(40, 50, 6), -- x, y, z
    }
}
