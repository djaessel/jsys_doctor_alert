-- JSYS was here! :P

Config = {
    KeyBinding = 0xD51B784F, -- key hash - E / INPUT_CONTEXT_Y
    --KeyBinding = 0xCEFD9220, -- key hash - E
    Range = 5, -- meters
    Cooldown = 40, -- seconds
    TickerCheck = 500, -- milliseconds
    KeyInfoVisibleDuration = 5, -- seconds

    CoordinatesAll = {
        {
            coords = vector3(10, 10, 1), -- x, y, z
            command = "doctorsdalert"
        },
        {
            coords = vector3(10, 20, 3), -- x, y, z
            command = "doctorsdalert"
        },
        {
            coords = vector3(40, 50, 6), -- x, y, z
            command = "doctorsdalert"
        },
    }
}
